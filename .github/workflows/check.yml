name: Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/astral-sh/uv:0.11.17-python3.12-trixie
    env:
      UV_CACHE_DIR: ${{ github.workspace }}/.uv-cache
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Check uv lockfile
        run: uv lock --check

      - name: Sync uv
        run: uv sync --frozen

      - name: Check
        run: uv run ruff check
